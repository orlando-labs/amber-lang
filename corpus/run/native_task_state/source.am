from sync import Atomic

def main():
  value = Atomic.new(1)
  worker = task.spawn:
    task.sleep(0.01)
    value.update: _1 + 2
  worker.wait()
  if worker.done? and worker.failed?.not:
    value.update: _1 + 1
  else:
    0
